#include<stdio.h>
#include<stdbool.h>

int main() {
    bool isRain = true;

    // If Else Statement

    if(!(isRain)) {
        printf("Bring Umbrella!!!");
    }
    else {
        printf("Not bring Umbrella !!!");
    }
    
    return 0;
}